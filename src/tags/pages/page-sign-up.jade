page-sign-up
  div.wrap-page.f.fh
    div.wrap-login-content
      div.wrap-logo.mb40
        img(src='./img/speculate-future.svg')
        //span Let's Tell a Lie
        //span to Speculate our Future.
        //img(src='./img/liard-first-logo.svg').logo
      div.wrap-button-login.f.fc.mb20
        span(onclick='{signUpWithFacebook}').button-fb.px14.py10 Sign Up with Facebook
      div.wrap-sign-up.f.fc
        a(href='./#sign-in').pb1 Sign In?

  style(scoped, type='less').
    :scope {
      .wrap-page {
        width: 100%;
        height: 100%;
        .wrap-logo {
          .logo {
            display: block;
            font-size: 32px;
            text-align: center;
          }
        }
        .wrap-input-group-name {
          border-bottom: solid #2a2a2a 0.8px;
          span {
            font-size: 14px;
          }
          input {
            font-size: 14px;
            width: 116px;
            outline: 0;
            border: 0px;
          }
        }
        .wrap-button-login {
          .button-fb {
            color: #FFF;
            font-size: 12px;
            text-align: center;
            letter-spacing: 0.8px;
            background: #4267B2;
            border-radius: 3px;
            cursor: pointer;
          }
          .prevent-action {
            pointer-events: none;
            background: #999;
          }
        }
        .wrap-sign-up {
          a {
            display: block;
            text-align: center;
            color: #999;
            font-weight: lighter;
            font-size: 12px;
            border-bottom: solid 0.8px;
          }
        }
      }
    }

  script.

    var self = this;

    this.on('mount', function(){

    });

    this.signUpWithFacebook = loginWithFacebook;

    /*
    this.letAction = 'prevent-action';
    
    this.up = function(e){

      var value = e.target.value;
      if(value==''){
        console.log('prevent action');
        self.letAction = 'prevent-action';
      }else{
        console.log('let action');
        self.letAction = 'let-action';
        session.groupName = value;
      }
      self.update();

    }
    */





